name: 🚀 PetPal CI/CD Master Pipeline

on:
  push:
    branches: [ "develop", "main" ]
  pull_request:
    branches: [ "main" ]

# Concurrencia: Si empujas código nuevo mientras corre un pipeline viejo, cancela el viejo para ahorrar tiempo.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: '20.x'

jobs:
  # --- FASE 1: CALIDAD Y PRUEBAS ---
  test-and-build:
    name: 🏗️ Build & Test
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 🟢 Node.js Setup (con Caché)
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: 📦 Instalar Dependencias
        run: npm ci

      - name: 🧪 Correr Tests (con Cobertura)
        run: npm test
        # Si esto falla, NADA se despliega.

  # --- FASE 2: DESPLIEGUE AUTOMÁTICO A QA ---
  deploy-qa:
    name: 🚀 Deploy QA
    needs: test-and-build
    if: github.ref == 'refs/heads/develop' # Solo corre en develop
    runs-on: ubuntu-latest
    environment: 
      name: qa
      url: https://petpal-backend-qa.up.railway.app
    
    steps:
      - name: 🚀 Simulación de Deploy a Railway (QA)
        run: echo "✅ Desplegando versión de prueba en QA..."

  # --- FASE 3: DESPLIEGUE A PRODUCCIÓN (CON CANDADO) ---
  deploy-production:
    name: 🛡️ Deploy Production
    needs: test-and-build
    if: github.ref == 'refs/heads/main' # Solo corre cuando el código YA está en main
    runs-on: ubuntu-latest
    environment: 
      name: production
      url: https://petpal-backend-prod.up.railway.app
    
    steps:
      - name: 🛑 Gatekeeper
        run: echo "🔐 Aprobación manual verificada."

      - name: 🚀 Simulación de Deploy a Railway (PROD)
        run: echo "✅ Desplegando versión final a Producción..."